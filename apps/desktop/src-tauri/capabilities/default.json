{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Core capability for the main assistant window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
