{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Core capability for the main assistant and click-through edge surfaces.",
  "windows": ["main", "edge-*"],
  "permissions": ["core:default"]
}
